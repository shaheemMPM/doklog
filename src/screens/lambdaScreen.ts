export const showLambdaScreen = async (region: string) => {
	console.log('\n=== AWS Lambda Screen ===');
	console.log(`Region: ${region}`);
	console.log('\nYou have reached the AWS Lambda screen');
};
